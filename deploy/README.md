# Deploy

BI-0101 создаёт только границу deployment-конфигурации.

Здесь пока **нет** staging/production deploy, server credentials, systemd units, Docker Compose или reverse proxy configuration.

Runtime deployment добавляется отдельной задачей после появления соответствующего implementation и live gate.
